kubeVersion: v1.28

repositories:
- name: elastic
  url: https://helm.elastic.co

releases:
- name: eck-stack
  namespace: elastic-system
  version: 0.12.1
  chart: elastic/eck-stack
  values:
    - values.yaml
